spring:
  application:
    name: caas-getway
server:
  port: 8010
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${eureka.cluster:http://localhost:8761/eureka/}
zuul:
  routes:
    caas-provider: /provider/**
    caas-consumer: /consumer/**