spring:
  application:
    name: caas-provider
server:
  port: 8000
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${eureka.cluster:http://localhost:8761/eureka/}